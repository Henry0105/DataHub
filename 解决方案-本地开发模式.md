# 解决方案：本地开发模式

## 🎯 两种开发模式对比

### 模式一：本地开发 + 服务器数据库（推荐用于团队协作）

```
本地电脑 ──网络──▶ 服务器数据库
```

**优点**：
- 多人共享同一数据库
- 数据一致性好
- 接近生产环境

**缺点**：
- 需要开放服务器端口（安全风险）
- 网络延迟影响开发体验
- 需要稳定的网络连接

### 模式二：本地开发 + 本地数据库（推荐用于单人开发）✅

```
本地电脑：前端 + 后端 + 数据库（全部在本地）
```

**优点**：
- 无需网络连接
- 响应速度快
- 开发体验好
- 无安全风险

**缺点**：
- 需要在本地安装MySQL和Redis
- 数据需要手动同步

## 💡 推荐方案：使用模式二（本地全套环境）

考虑到：
1. 你是单人开发
2. 服务器端口开放有安全风险
3. 本地开发体验更好

**建议使用本地数据库进行开发！**

## 🚀 快速设置本地环境

### 步骤1：安装本地MySQL

```bash
# 使用Homebrew安装MySQL
brew install mysql

# 启动MySQL服务
brew services start mysql

# 设置root密码（可选）
mysql_secure_installation
```

### 步骤2：安装本地Redis（可选）

```bash
# 使用Homebrew安装Redis
brew install redis

# 启动Redis服务
brew services start redis
```

### 步骤3：初始化本地数据库

```bash
# 登录MySQL
mysql -u root -p

# 执行初始化脚本
source /Users/liumingze/DataHub/datahub-backend/sql/datahub.sql;

# 验证
SHOW DATABASES LIKE 'datahub';
USE datahub;
SHOW TABLES;
exit;
```

### 步骤4：修改后端配置为本地

编辑 `datahub-backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/datahub?...
      username: root
      password: root  # 你的本地MySQL密码
  
  redis:
    host: localhost
    port: 6379
    password:  # 本地Redis通常无密码
```

### 步骤5：启动服务

```bash
# 终端1：启动后端
cd /Users/liumingze/DataHub/datahub-backend
export JAVA_HOME=$(/usr/libexec/java_home)
mvn spring-boot:run

# 终端2：前端已在运行
# 直接访问 http://localhost:5173
```

## 📋 完整的本地配置文件

我帮你创建一个本地开发专用的配置文件：

`datahub-backend/src/main/resources/application-local.yml`：

```yaml
spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/datahub?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: root
      password: root
  
  redis:
    host: localhost
    port: 6379
    password:
```

启动时指定使用本地配置：

```bash
mvn spring-boot:run -Dspring-boot.run.profiles=local
```

## 🔄 数据同步方案

如果需要将本地数据同步到服务器：

### 方案1：导出导入SQL

```bash
# 1. 导出本地数据
mysqldump -u root -p datahub > datahub_local.sql

# 2. 上传到服务器
scp datahub_local.sql datahub-db:/tmp/

# 3. 在服务器上导入
ssh datahub-db
docker exec -i datahub-mysql mysql -uroot -pDataHub@2025 datahub < /tmp/datahub_local.sql
```

### 方案2：使用数据库同步工具

- Navicat
- DBeaver
- MySQL Workbench

## 🎯 最终推荐

### 开发阶段（现在）

使用 **本地全套环境**：
- ✅ 本地MySQL
- ✅ 本地Redis
- ✅ 本地后端
- ✅ 本地前端

### 部署阶段（完成开发后）

将代码部署到服务器：
- ✅ 服务器MySQL（已有）
- ✅ 服务器Redis（已有）
- ✅ 服务器后端（待部署）
- ✅ 服务器前端（待部署）

## 📝 下一步操作

请选择一种方案：

### 方案A：使用本地数据库（推荐）✅

```bash
# 1. 安装MySQL
brew install mysql
brew services start mysql

# 2. 初始化数据库
mysql -u root -p < /Users/liumingze/DataHub/datahub-backend/sql/datahub.sql

# 3. 修改配置为localhost
# 编辑 application.yml

# 4. 启动后端
cd datahub-backend
mvn spring-boot:run
```

### 方案B：开放服务器端口（不推荐）

需要在腾讯云控制台：
1. 进入安全组设置
2. 添加入站规则
3. 开放3306端口（MySQL）
4. 开放6379端口（Redis）

**安全风险**：数据库直接暴露在公网

## 💡 我的建议

**现在就使用方案A（本地数据库）**，原因：

1. ✅ 快速开始开发，无需等待网络
2. ✅ 开发体验好，响应速度快
3. ✅ 无安全风险
4. ✅ 开发完成后再部署到服务器

你觉得呢？我可以帮你：
- 安装配置本地MySQL
- 修改配置文件
- 启动后端服务

