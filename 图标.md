# 🌐 语言切换 & 🎨 主题切换 - 完整指南

本文档详细介绍了项目中的国际化（中英文切换）和主题切换（亮色/暗色模式）功能的实现原理和使用方法。

---

## 📋 目录

- [1. 语言切换（国际化）](#1-语言切换国际化)
  - [1.1 核心文件](#11-核心文件)
  - [1.2 实现原理](#12-实现原理)
  - [1.3 使用方法](#13-使用方法)
- [2. 主题切换](#2-主题切换)
  - [2.1 核心文件](#21-核心文件)
  - [2.2 实现原理](#22-实现原理)
  - [2.3 使用方法](#23-使用方法)
- [3. 技术栈](#3-技术栈)
- [4. 最佳实践](#4-最佳实践)

---

## 1. 语言切换（国际化）

### 1.1 核心文件

```
src/
├── locale/
│   ├── index.ts           # 国际化配置入口
│   ├── zh-CN.ts          # 中文语言包
│   └── en-US.ts          # 英文语言包
├── hooks/
│   └── locale.ts         # 语言切换 Hook
├── App.vue               # 应用根组件
└── components/
    └── navbar/
        └── index.vue     # 导航栏（包含语言切换按钮）
```

### 1.2 实现原理

#### 📁 `src/locale/index.ts` - 国际化配置

```typescript
import { createI18n } from 'vue-i18n';
import en from './en-US';
import cn from './zh-CN';

// 语言选项
export const LOCALE_OPTIONS = [
  { label: '中文', value: 'zh-CN' },
  { label: 'English', value: 'en-US' },
];

// 从本地存储获取默认语言，没有则使用中文
const defaultLocale = localStorage.getItem('arco-locale') || 'zh-CN';

// 创建 i18n 实例
const i18n = createI18n({
  locale: defaultLocale,
  fallbackLocale: 'en-US',
  legacy: false,
  allowComposition: true,
  messages: {
    'en-US': en,
    'zh-CN': cn,
  },
});

export default i18n;
```

#### 📁 `src/hooks/locale.ts` - 语言切换 Hook

```typescript
import { computed } from 'vue';
import { useI18n } from 'vue-i18n';
import { Message } from '@arco-design/web-vue';

export default function useLocale() {
  const i18 = useI18n();
  
  // 当前语言
  const currentLocale = computed(() => {
    return i18.locale.value;
  });
  
  // 切换语言
  const changeLocale = (value: string) => {
    if (i18.locale.value === value) {
      return;
    }
    i18.locale.value = value;
    localStorage.setItem('arco-locale', value); // 保存到本地存储
    Message.success(i18.t('navbar.action.locale'));
  };
  
  return {
    currentLocale,
    changeLocale,
  };
}
```

#### 📁 `src/App.vue` - 应用根组件配置

```vue
<template>
  <a-config-provider :locale="locale">
    <router-view />
    <global-setting />
  </a-config-provider>
</template>

<script lang="ts" setup>
  import { computed } from 'vue';
  import enUS from '@arco-design/web-vue/es/locale/lang/en-us';
  import zhCN from '@arco-design/web-vue/es/locale/lang/zh-cn';
  import GlobalSetting from '@/components/global-setting/index.vue';
  import useLocale from '@/hooks/locale';

  const { currentLocale } = useLocale();
  
  // 根据当前语言返回 Arco Design 的语言包
  const locale = computed(() => {
    switch (currentLocale.value) {
      case 'zh-CN':
        return zhCN;
      case 'en-US':
        return enUS;
      default:
        return enUS;
    }
  });
</script>
```

#### 📁 `src/components/navbar/index.vue` - 导航栏语言切换按钮

```vue
<template>
  <!-- 语言切换按钮 -->
  <li>
    <a-tooltip :content="$t('settings.language')">
      <a-button
        class="nav-btn"
        type="outline"
        :shape="'circle'"
        @click="setDropDownVisible"
      >
        <template #icon>
          <icon-language />
        </template>
      </a-button>
    </a-tooltip>
    
    <!-- 语言下拉菜单 -->
    <a-dropdown trigger="click" @select="changeLocale as any">
      <div ref="triggerBtn" class="trigger-btn"></div>
      <template #content>
        <a-doption
          v-for="item in locales"
          :key="item.value"
          :value="item.value"
        >
          <template #icon>
            <!-- 当前选中的语言显示勾选图标 -->
            <icon-check v-show="item.value === currentLocale" />
          </template>
          {{ item.label }}
        </a-doption>
      </template>
    </a-dropdown>
  </li>
</template>

<script lang="ts" setup>
  import { ref } from 'vue';
  import { LOCALE_OPTIONS } from '@/locale';
  import useLocale from '@/hooks/locale';

  const { changeLocale, currentLocale } = useLocale();
  const locales = [...LOCALE_OPTIONS];
  const triggerBtn = ref();
  
  // 触发下拉菜单显示
  const setDropDownVisible = () => {
    const event = new MouseEvent('click', {
      view: window,
      bubbles: true,
      cancelable: true,
    });
    triggerBtn.value.dispatchEvent(event);
  };
</script>
```

### 1.3 使用方法

#### 在组件中使用语言切换

```vue
<script setup>
import useLocale from '@/hooks/locale';

const { currentLocale, changeLocale } = useLocale();

// 获取当前语言
console.log(currentLocale.value); // 'zh-CN' 或 'en-US'

// 切换到英文
const switchToEnglish = () => {
  changeLocale('en-US');
};

// 切换到中文
const switchToChinese = () => {
  changeLocale('zh-CN');
};
</script>
```

#### 在模板中使用国际化文本

```vue
<template>
  <!-- 使用 $t 函数 -->
  <div>{{ $t('menu.dashboard') }}</div>
  
  <!-- 在属性中使用 -->
  <a-button :title="$t('common.submit')">
    {{ $t('common.submit') }}
  </a-button>
  
  <!-- 带参数的翻译 -->
  <div>{{ $t('message.welcome', { name: 'John' }) }}</div>
</template>
```

#### 添加新的语言文本

**中文语言包 (`src/locale/zh-CN.ts`):**

```typescript
export default {
  'menu.dashboard': '仪表盘',
  'menu.datasource': '数据源管理',
  'common.submit': '提交',
  'common.cancel': '取消',
  // ... 更多翻译
};
```

**英文语言包 (`src/locale/en-US.ts`):**

```typescript
export default {
  'menu.dashboard': 'Dashboard',
  'menu.datasource': 'Datasource',
  'common.submit': 'Submit',
  'common.cancel': 'Cancel',
  // ... 更多翻译
};
```

---

## 2. 主题切换

### 2.1 核心文件

```
src/
├── store/
│   └── modules/
│       └── app/
│           ├── index.ts      # 应用状态管理（包含主题）
│           └── types.ts      # 类型定义
├── hooks/
│   └── themes.ts            # 主题 Hook
└── components/
    └── navbar/
        └── index.vue        # 导航栏（包含主题切换按钮）
```

### 2.2 实现原理

#### 📁 `src/store/modules/app/index.ts` - 主题状态管理

```typescript
import { defineStore } from 'pinia';
import type { AppState } from './types';
import defaultSettings from '@/config/settings.json';

const useAppStore = defineStore('app', {
  state: (): AppState => ({ ...defaultSettings }),
  
  getters: {
    appCurrentSetting(state: AppState): AppState {
      return { ...state };
    },
    appDevice(state: AppState) {
      return state.device;
    },
  },
  
  actions: {
    // 更新设置
    updateSettings(partial: Partial<AppState>) {
      this.$patch(partial);
    },

    // 切换主题
    toggleTheme(dark: boolean) {
      if (dark) {
        this.theme = 'dark';
        document.body.setAttribute('arco-theme', 'dark');
      } else {
        this.theme = 'light';
        document.body.removeAttribute('arco-theme');
      }
    },
    
    toggleDevice(device: string) {
      this.device = device;
    },
  },
});

export default useAppStore;
```

#### 📁 `src/hooks/themes.ts` - 主题 Hook

```typescript
import { computed } from 'vue';
import { useAppStore } from '@/store';

export default function useThemes() {
  const appStore = useAppStore();
  
  // 是否为暗色主题
  const isDark = computed(() => {
    return appStore.theme === 'dark';
  });
  
  return {
    isDark,
  };
}
```

#### 📁 `src/components/navbar/index.vue` - 导航栏主题切换按钮

```vue
<template>
  <!-- 主题切换按钮 -->
  <li>
    <a-tooltip
      :content="
        theme === 'light'
          ? $t('settings.navbar.theme.toDark')
          : $t('settings.navbar.theme.toLight')
      "
    >
      <a-button
        class="nav-btn"
        type="outline"
        :shape="'circle'"
        @click="handleToggleTheme"
      >
        <template #icon>
          <!-- 暗色模式显示月亮图标 -->
          <icon-moon-fill v-if="theme === 'dark'" />
          <!-- 亮色模式显示太阳图标 -->
          <icon-sun-fill v-else />
        </template>
      </a-button>
    </a-tooltip>
  </li>
</template>

<script lang="ts" setup>
  import { computed } from 'vue';
  import { useDark, useToggle } from '@vueuse/core';
  import { useAppStore } from '@/store';

  const appStore = useAppStore();
  
  // 当前主题
  const theme = computed(() => {
    return appStore.theme;
  });
  
  // 使用 VueUse 的 useDark 和 useToggle
  const isDark = useDark({
    selector: 'body',
    attribute: 'arco-theme',
    valueDark: 'dark',
    valueLight: 'light',
    storageKey: 'arco-theme', // 保存到 localStorage
    onChanged(dark: boolean) {
      // 主题变化时更新 store
      appStore.toggleTheme(dark);
    },
  });
  
  const toggleTheme = useToggle(isDark);
  
  // 切换主题
  const handleToggleTheme = () => {
    toggleTheme();
  };
</script>
```

### 2.3 使用方法

#### 在组件中使用主题切换

```vue
<script setup>
import useThemes from '@/hooks/themes';
import { useAppStore } from '@/store';

const appStore = useAppStore();
const { isDark } = useThemes();

// 检查当前主题
console.log(isDark.value); // true 或 false
console.log(appStore.theme); // 'dark' 或 'light'

// 切换到暗色模式
const switchToDark = () => {
  appStore.toggleTheme(true);
};

// 切换到亮色模式
const switchToLight = () => {
  appStore.toggleTheme(false);
};

// 切换主题（自动判断）
const toggleTheme = () => {
  appStore.toggleTheme(!isDark.value);
};
</script>
```

#### 根据主题应用不同样式

```vue
<template>
  <div :class="{ 'dark-mode': isDark }">
    <h1>标题</h1>
  </div>
</template>

<script setup>
import useThemes from '@/hooks/themes';

const { isDark } = useThemes();
</script>

<style scoped>
.dark-mode {
  background-color: #1a1a1a;
  color: #ffffff;
}
</style>
```

#### 使用 CSS 变量（推荐）

Arco Design 提供了完整的 CSS 变量系统，会根据主题自动切换：

```vue
<style scoped>
.my-component {
  /* 这些变量会根据主题自动变化 */
  background-color: var(--color-bg-1);
  color: var(--color-text-1);
  border: 1px solid var(--color-border);
}

.my-button {
  background: var(--color-primary-light-1);
  color: var(--color-primary-6);
}
</style>
```

**常用 CSS 变量：**

| 变量名 | 说明 |
|--------|------|
| `--color-bg-1` | 主背景色 |
| `--color-bg-2` | 次级背景色 |
| `--color-text-1` | 主文本色 |
| `--color-text-2` | 次级文本色 |
| `--color-border` | 边框颜色 |
| `--color-primary-6` | 主题色 |
| `--color-success-6` | 成功色 |
| `--color-warning-6` | 警告色 |
| `--color-danger-6` | 危险色 |

---

## 3. 技术栈

### 语言切换
- **Vue I18n** - Vue 3 国际化框架
- **localStorage** - 持久化存储用户语言选择
- **Arco Design 语言包** - UI 组件库的国际化支持

### 主题切换
- **VueUse** - `useDark` 和 `useToggle` 组合式 API
- **Pinia** - Vue 3 状态管理
- **Arco Design 主题系统** - 基于 CSS 变量的主题切换
- **localStorage** - 持久化存储主题选择

---

## 4. 最佳实践

### ✅ 语言切换最佳实践

1. **统一管理语言文本**
   - 所有文本都应该放在语言包中，不要硬编码
   - 使用有意义的 key，如 `menu.dashboard` 而不是 `text1`

2. **保持语言包同步**
   - 添加新文本时，确保所有语言包都有对应翻译
   - 缺失的翻译会使用 fallback 语言（en-US）

3. **使用命名空间**
   ```typescript
   // 好的做法
   'menu.dashboard': '仪表盘',
   'menu.datasource': '数据源',
   'common.submit': '提交',
   
   // 不好的做法
   'dashboard': '仪表盘',
   'datasource': '数据源',
   'submit': '提交',
   ```

4. **处理复数和参数**
   ```typescript
   // 语言包
   'message.itemCount': '共 {count} 项',
   
   // 使用
   $t('message.itemCount', { count: 10 })
   ```

### ✅ 主题切换最佳实践

1. **优先使用 CSS 变量**
   - 使用 Arco Design 提供的 CSS 变量
   - 避免硬编码颜色值

2. **提供平滑过渡**
   ```css
   .my-component {
     background-color: var(--color-bg-1);
     transition: background-color 0.3s ease;
   }
   ```

3. **测试两种主题**
   - 确保在亮色和暗色模式下都能正常显示
   - 注意对比度和可读性

4. **自定义主题色**
   ```typescript
   // 在组件中根据主题动态调整
   const getThemeColor = () => {
     return isDark.value ? '#1890ff' : '#165dff';
   };
   ```

### ✅ 性能优化

1. **懒加载语言包**
   ```typescript
   // 按需加载语言包
   const loadLocaleMessages = async (locale: string) => {
     const messages = await import(`./locale/${locale}.ts`);
     i18n.global.setLocaleMessage(locale, messages.default);
   };
   ```

2. **缓存主题状态**
   - 使用 localStorage 缓存用户选择
   - 页面刷新时自动恢复

3. **避免频繁切换**
   - 使用防抖处理主题切换
   - 减少不必要的 DOM 操作

---

## 🎯 核心特性总结

### 语言切换
✅ **自动持久化** - 用户选择自动保存到 localStorage  
✅ **响应式** - 切换后立即生效，无需刷新  
✅ **全局同步** - 所有组件自动更新  
✅ **组件库支持** - Arco Design 组件自动切换语言  
✅ **类型安全** - 完整的 TypeScript 支持  

### 主题切换
✅ **自动持久化** - 主题选择自动保存  
✅ **响应式** - 切换后立即生效  
✅ **全局同步** - 所有组件自动更新  
✅ **CSS 变量** - 基于 Arco Design 主题系统  
✅ **优雅降级** - 默认使用亮色主题  

---

## 📞 常见问题

### Q1: 如何添加新的语言？

1. 在 `src/locale/` 下创建新的语言文件，如 `ja-JP.ts`
2. 在 `src/locale/index.ts` 中导入并注册
3. 在 `LOCALE_OPTIONS` 中添加选项
4. 在 `src/App.vue` 中添加对应的 Arco Design 语言包

### Q2: 主题切换后某些颜色没有变化？

确保使用了 Arco Design 的 CSS 变量，而不是硬编码的颜色值。

### Q3: 如何自定义主题颜色？

可以在 `src/config/settings.json` 中配置主题色，或者使用 Arco Design 的主题配置功能。

### Q4: 语言切换后某些文本没有变化？

检查是否在语言包中添加了对应的翻译，确保使用了 `$t()` 函数而不是硬编码文本。

---

## 📚 相关资源

- [Vue I18n 官方文档](https://vue-i18n.intlify.dev/)
- [VueUse 官方文档](https://vueuse.org/)
- [Arco Design Vue 主题配置](https://arco.design/vue/docs/theme)
- [Pinia 官方文档](https://pinia.vuejs.org/)

---

**最后更新时间：** 2025-11-11  
**作者：** MGX 数据平台团队

