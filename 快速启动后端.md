# 快速启动后端服务

## 🔴 当前问题

**前端报错**：网络错误，请检查网络连接
**原因**：后端服务未启动

## ✅ 解决步骤

### 步骤1：初始化数据库

```bash
# 登录MySQL（请替换为你的实际密码）
mysql -u root -p

# 在MySQL中执行
source /Users/liumingze/DataHub/datahub-backend/sql/datahub.sql;

# 验证数据库是否创建成功
SHOW DATABASES LIKE 'datahub';
USE datahub;
SHOW TABLES;

# 退出MySQL
exit;
```

### 步骤2：修改后端配置

编辑文件：`/Users/liumingze/DataHub/datahub-backend/src/main/resources/application.yml`

找到这一行并修改为你的MySQL密码：

```yaml
spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/datahub?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: root
      password: 你的MySQL密码  # 修改这里！
```

### 步骤3：启动后端服务

```bash
# 进入后端目录
cd /Users/liumingze/DataHub/datahub-backend

# 启动服务（会自动下载依赖，首次启动较慢）
mvn spring-boot:run
```

**启动成功的标志**：

```
========================================
    DataHub 数据中台启动成功！
    接口文档: http://localhost:8080/api/doc.html
    Druid监控: http://localhost:8080/api/druid
========================================
```

### 步骤4：测试登录

打开浏览器，访问：http://localhost:5173

- 用户名：`admin`
- 密码：`admin123`

## 🔍 问题排查

### 问题1：MySQL密码错误

**错误信息**：
```
Access denied for user 'root'@'localhost'
```

**解决方法**：
1. 确认MySQL密码
2. 修改 `application.yml` 中的密码
3. 重启后端服务

### 问题2：数据库未创建

**错误信息**：
```
Unknown database 'datahub'
```

**解决方法**：
执行步骤1，初始化数据库

### 问题3：端口被占用

**错误信息**：
```
Port 8080 is already in use
```

**解决方法**：
```bash
# 查找占用8080端口的进程
lsof -i:8080

# 杀死进程
kill -9 PID
```

### 问题4：Maven下载依赖慢

**解决方法**：
配置国内镜像，编辑 `~/.m2/settings.xml`：

```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

## 📝 快速测试命令

### 测试后端是否启动

```bash
curl http://localhost:8080/api/auth/login \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

**成功响应**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGc...",
    "userId": 1,
    "username": "admin",
    "nickname": "管理员"
  }
}
```

### 测试数据库连接

```bash
mysql -u root -p -e "USE datahub; SELECT * FROM sys_user;"
```

## 🎯 完整启动流程

```bash
# 1. 初始化数据库（首次运行）
mysql -u root -p < /Users/liumingze/DataHub/datahub-backend/sql/datahub.sql

# 2. 修改配置文件中的MySQL密码
# 编辑 datahub-backend/src/main/resources/application.yml

# 3. 启动后端（新终端）
cd /Users/liumingze/DataHub/datahub-backend
mvn spring-boot:run

# 4. 前端已经在运行，直接访问
# http://localhost:5173
# 用户名：admin，密码：admin123
```

## 💡 提示

1. **首次启动会比较慢**，因为需要下载Maven依赖
2. **确保MySQL正在运行**：`mysql.server status` 或 `brew services list`
3. **确保数据库已初始化**：登录MySQL查看是否有datahub数据库
4. **后端启动后不要关闭终端**，否则服务会停止

## 🆘 如果还是不行

请提供以下信息：

1. MySQL是否正在运行？
   ```bash
   mysql.server status
   ```

2. 数据库是否已创建？
   ```bash
   mysql -u root -p -e "SHOW DATABASES;"
   ```

3. 后端启动时的错误信息？
   ```bash
   cd /Users/liumingze/DataHub/datahub-backend
   mvn spring-boot:run
   # 复制所有错误信息
   ```

4. 8080端口是否被占用？
   ```bash
   lsof -i:8080
   ```

