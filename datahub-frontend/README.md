# AllData数据中台 - 前端系统

## 项目简介

AllData数据中台前端系统，基于 Vue 3 + TypeScript + Vite + Element Plus 构建的企业级数据管理平台。

## 技术栈

- **框架**: Vue 3.4+ (Composition API)
- **语言**: TypeScript 5.3+
- **构建工具**: Vite 5.0+
- **UI组件库**: Element Plus 2.5+
- **状态管理**: Pinia 2.1+
- **路由**: Vue Router 4.2+
- **HTTP客户端**: Axios 1.6+
- **图表**: ECharts 5.4+
- **样式**: SCSS

## 功能模块

### 核心平台
- 🏠 **首页看板** - 数据统计与快捷入口
- 🔐 **用户登录** - 用户认证与授权

### 数据管理
- 📊 **数据源平台 (Chat2DB)** - AI驱动的数据库管理
- 🔄 **数据库同步 (DBSwitch)** - 多数据库迁移同步
- 💎 **数据中枢 (DataCap)** - 统一数据访问接口
- 🔗 **数据集成 (Transport)** - 基于DataX的数据集成
- 🌊 **数据集成平台 (Tis)** - 实时批量数据同步
- 🚀 **数据同步平台 (SeaTunnel)** - 高性能数据集成

### 大数据平台
- 🎯 **数据平台 (DataSophon)** - 大数据集群管理
- ⚡ **实时开发 (StreamPark)** - Flink/Spark作业管理

### 数据治理
- 🔍 **数据比对** - 多源数据一致性校验
- 📋 **数据标准** - 数据标准定义与管理

## 快速开始

### 环境要求

- Node.js >= 16.x
- npm >= 8.x 或 pnpm >= 8.x

### 安装依赖

```bash
# 使用 npm
npm install

# 或使用 pnpm (推荐)
pnpm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:5173

默认账号: `admin` / `admin123`

### 生产构建

```bash
npm run build:prod
```

构建产物位于 `dist` 目录

### 预览构建结果

```bash
npm run preview
```

## 项目结构

```
alldata-frontend/
├── public/              # 静态资源
├── src/
│   ├── api/            # API接口
│   ├── assets/         # 资源文件
│   ├── components/     # 公共组件
│   ├── layouts/        # 布局组件
│   ├── router/         # 路由配置
│   ├── store/          # 状态管理
│   ├── styles/         # 全局样式
│   ├── utils/          # 工具函数
│   ├── views/          # 页面组件
│   ├── App.vue         # 根组件
│   └── main.ts         # 入口文件
├── .env                # 环境变量
├── .env.development    # 开发环境变量
├── .env.production     # 生产环境变量
├── index.html          # HTML模板
├── package.json        # 项目配置
├── tsconfig.json       # TS配置
└── vite.config.ts      # Vite配置
```

## 开发规范

### 代码风格

- 使用 TypeScript 进行类型约束
- 使用 Composition API 编写组件
- 使用 ESLint 进行代码检查
- 使用 Prettier 进行代码格式化

### 命名规范

- 组件文件: PascalCase (如 `UserList.vue`)
- 工具文件: camelCase (如 `request.ts`)
- 常量: UPPER_SNAKE_CASE (如 `API_BASE_URL`)

### Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
perf: 性能优化
test: 测试相关
chore: 构建/工具链相关
```

## 浏览器支持

- Chrome >= 90
- Firefox >= 88
- Safari >= 14
- Edge >= 90

## 许可证

Copyright © 2025 AllData Team

