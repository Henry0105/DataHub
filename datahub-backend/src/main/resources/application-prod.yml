spring:
  # 生产环境数据源配置
  datasource:
    druid:
      url: jdbc:mysql://10.2.0.16:3306/datahub?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: root
      password: DataHub@2025
  
  # 生产环境Redis配置
  redis:
    host: 10.2.0.16
    port: 6379
    password: DataHub@2025

# 生产环境日志配置
logging:
  level:
    com.datahub: info
    com.baomidou.mybatisplus: warn
  file:
    name: /app/logs/datahub-backend.log
    max-size: 100MB
    max-history: 30

